"""Bandit (security) and Pylint (style/errors) wrapper functions.

Runs static analysis tools and parses their JSON output.
"""

# TODO: Implement:
#   - run_bandit(repo_path: str) -> list[dict]
#   - run_pylint(repo_path: str) -> list[dict]
#   - run_all_static_analysis(repo_path: str) -> dict
